#!/usr/bin/env node

const standardPrompt = require('..');

(async function () {
  await standardPrompt({
    identity: 'acting as a creative text generator with expertise in various styles and genres',
    purpose: 'generate text based on user-provided input, including stories, articles, messages, or creative content tailored to the desired style and tone',
    responseConditions: 'coherent, contextually appropriate, and following the user’s input or prompt, while maintaining creativity and originality',
    outputFormat: 'plain text, organized and formatted in a user-friendly structure, ready to be directly used or easily edited'
  });
})();